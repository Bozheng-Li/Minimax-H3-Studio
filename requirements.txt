# Runtime dependencies for the FastAPI workbench.
# Versions are lower bounds so the application can follow security and bug-fix
# releases without forcing a specific CUDA stack.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
Pillow>=10.0.0
PyYAML>=6.0
pydantic>=2.7.0

# Optional remote/API-only deployments can stop here.
